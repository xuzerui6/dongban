# 动伴 Motion Buddy — 48 小时 Web Demo 实施计划

## Context

你要做一个手机网页端的 AI 运动陪伴产品，体验类似豆包视频通话：用户一个人训练时，通过唤醒词「你好动伴」叫起大模型，摄像头识别动作、自动计数、给出指导和情绪反馈，训练成果转化为数字分身的装备与成就。

桌面上两份文档需要对齐：`动伴_Motion_Buddy_48小时黑客松_Web_Demo_PRD与开发文档(1).docx`（v1.0，有评分/多动作）和 `动伴_Motion_Buddy_最终项目需求与技术实施文档_v2.0.docx`（v2.0，冻结了「只做深蹲、不评分、不评判」）。两份互相矛盾，文档内嵌的 UI 参考图也还画着 `动作评分 92% PERFECT`。

已确认的四项决策：

| 项 | 决策 |
|---|---|
| 产品边界 | 折中：**固定动作集自动识别 + 温和提示**，不做 0–100 评分、不做 PERFECT 标签 |
| 交付范围 | 48 小时黑客松 Demo，非核心功能可 Mock |
| 卡路里 | **确定性公式**（有心率带用 Keytel，无心率带用 MET），大模型只负责表达 |
| 技术栈 | **Node/TS 全栈 + Cloudflare Workers**（替代文档里的 FastAPI） |

目标产出：一个公网 URL，手机或桌面浏览器打开即可跑通完整闭环——唤醒动伴 → 说出想训练 → 授权摄像头 → 自动识别动作并计数 → 语音陪伴与提示 → 结算卡路里与 XP → 解锁装备 → 数字分身换装。

---

## 一、产品边界（本版最终版，覆盖两份文档）

以 v2.0 的「不评判」精神为底，但放开动作范围。相对两份文档的明确改动：

**采纳 v1.0 的**：多动作、自动识别、实时提示、心率带、装备体系与稀有度表。

**采纳 v2.0 的**：不做 0–100 分数、不做「标准动作」标签、本地优先计数、事实与表达分离、策略网关、视觉默认不上传、状态机由代码控制。

**新增（两份文档都没有）**：
- 唤醒词 `你好动伴` 唤起对话
- 用户表达运动意图后，AI **主动询问是否开启摄像头**，同意才调用视觉
- 分身体型随 BMI 变化（胖瘦形态，不只是换装）
- 首次进入用身高体重生成分身

**替换评分的表达方式**（这是 D1 的落地）：屏幕上显示 `识别置信度` 和 `节奏` 这类事实，不显示分数。提示语用具体、可执行、带不确定性措辞的句子，例如「膝盖再往外一点点」，而不是「你的动作只有 72 分」。

### 支持的动作集（按算法成熟度分档）

调研查遍开源生态后的结论：**只有深蹲和俯卧撑有被验证过的角度阈值**，其余要自己调。所以动作集分三档交付，不平铺开：

| 档 | 动作 | 计数信号（三点夹角） | 底 / 顶 | MET |
|---|---|---|---|---|
| **P0** | 深蹲 squat | 髋-**膝**-踝 | 110° / 160° | 5.0 |
| **P0** | 俯卧撑 pushup | 肩-**肘**-腕 | 100° / 160° | 8.0 |
| **P1** | 仰卧起坐 situp | 躯干与地面夹角（**极性反转**） | 150° / 50° | 5.0 |
| **P1** | 原地跑/高抬腿 running | 膝抬高交替频率 | 需调 | 8.0 |
| **P2** | 开合跳 jumping_jack | 腕高于头 + 踝间距同相位 | 无既有算法 | 8.0 |
| **P2** | 俄罗斯转体 russian_twist | 肩中点相对髋中点的水平偏移 | 无既有算法 | 4.0 |

两个必须提前知道的坑：

1. **仰卧起坐/弯举的极性是反的**——伸展位才是「底」，屈曲角度才计数。状态机必须每个动作带一个方向标志，不能硬编码「角度减小 = 下降」。
2. **开合跳和俄罗斯转体没有任何开源实现可参考**。开合跳是快速动作，在 25fps 下会严重走样；俄罗斯转体是旋转信号，2D 单摄像头**可能根本无法可靠计数**。这两个放 P2，真调不出来就退回手动计数，不硬撑。

难度分档阈值（新手友好，避免一开始就判不过）：

| 动作 | 新手 | 进阶 | 高手 |
|---|---|---|---|
| 俯卧撑 | 100 / 155 | 90 / 160 | 80 / 165 |
| 深蹲 | 110 / 160 | 95 / 165 | 80 / 170 |
| 仰卧起坐 | 150 / 50 | 160 / 40 | 165 / 30 |

### 动作识别用规则，不用机器学习

调研建议干脆取消自动识别、让用户手选。**不采纳**——自动识别是你明确要的产品差异点。但也不做 ML 分类器：MediaPipe 官方的 k-NN 姿态嵌入方案需要「每个动作每个终止状态几百个样本」，48 小时内不可能采集。

改用**启发式规则分类**：这 6 个动作的姿态特征差异极大，几个判据就能分开——躯干是竖直还是水平、髋部有没有大幅垂直位移、腕踝是否同相位振荡、肩线有没有水平旋转。几十行代码，零训练数据，可单测，判错了也能解释原因。

置信度不足时**不硬猜**：进入 `UNKNOWN`，先抽 1 帧问 `qwen3-vl-flash`，仍不确定就由动伴开口问「你现在做的是什么动作？我没看清」，用户口头指定后锁定。这条路径同时满足通用性和「不伪装准确」的边界。

### 计数状态机

**没有可用的 JS 计数库**——调研查遍生态，star 高的仓库大多是 Python 学生项目，且不少**根本没有 license**（`SuryaThejas-07/Virtual_Fitness_Trainer` 42 star 无 license、`materight/RepNet-pytorch` 41 star 无 license），法律上不可用。唯一能直接抄代码的是 `abinashstack/ai-form-checker`（**MIT，0 star，JavaScript**，浏览器端 MediaPipe）——star 数在这个领域完全不能当质量信号。

采用它的**四状态**机（比常见的两状态 up/down 好，因为能拿到真实底点）：

```
TOP → DESCENDING → BOTTOM → ASCENDING → TOP(+1)
```

- 迟滞margin ±5°，防止在阈值附近抖动重复计数
- 在 DESCENDING/BOTTOM 阶段**跟踪运行最小值**，这是拿到真实底点用于深度提示的关键
- 关键点置信度不足或人离开画面 → `PAUSED`，恢复后重新校准，**不补算**

**必须重写为增量式**：原仓库的 `detectReps(frames, exercise)` 接收整个帧缓冲从头重扫，是为分析上传视频设计的，不能直接用于实时流。状态机模式照搬，消费单帧的在线版本要自己写。

**平滑用 One Euro 滤波器**，不用原仓库的中心移动平均——后者是非因果的（要看未来帧），实时场景根本用不了。One Euro 会随运动速度自适应截止频率，正好避免在动作折返点上「延迟 vs 抖动」二选一。

### 动作提示阈值（替代评分）

这些是 §1 承诺的「具体、可执行」提示的数据来源。都从仓库源码核对过：

| 检查 | 信号 | 判据 | 提示语 |
|---|---|---|---|
| 深蹲深度 | 膝角最小值 | <100° 充分 / 100–130° 临界 / >130° 不足 | 「这组下得挺深」/「还能再低一点点」 |
| 深蹲前倾 | 躯干倾角最大值 | >55° | 「核心收一下，往后坐」 |
| 俯卧撑塌腰 | 肩-髋-踝 | <160° | 「腰再收一点，身体成一条线」 |
| 俯卧撑深度 | 肘角最小值 | <95° 充分 / 95–120° 临界 / >120° 偏浅 | 「胸再往下一点」 |

用 30 帧滚动历史避免结论闪烁。**内部可以算这些判据，但绝不把它们合成一个 0–100 分数显示给用户**——这是 D1 的底线，也是为什么提示语写成动作建议而不是扣分说明。

### 单摄像头的取舍（必须告诉用户机位）

**膝内扣（knee valgus）做不了**，这点要直说：所有调研到的仓库都没实现它，原因是几何上的——膝内扣只在**正面**视角可观测，而深度和背角需要**侧面**视角。一个摄像头位置无法同时评估两者。

所以每个动作指定一个机位，并在准备页明确告诉用户：

| 动作 | 机位 | 能给的提示 |
|---|---|---|
| 深蹲 | 侧面 | 深度、前倾 |
| 俯卧撑 | 侧面 | 深度、塌腰 |
| 仰卧起坐 | 侧面 | 幅度 |
| 开合跳 / 俄罗斯转体 | 正面 | 仅计数 |

膝内扣检测列为后续研究项，不在本版承诺范围内。

---

## 二、架构

```
浏览器 (React + Vite + TS, 390×844 手机画布)
  摄像头 → MediaPipe PoseLandmarker (Web Worker) → 关键点
                                                     ↓
                              packages/motion-core (纯 TS, 无 IO)
                                ├─ classifier    识别哪个动作
                                ├─ rep-counter   状态机计数
                                ├─ form-hints    温和提示规则
                                └─ calories      确定性公式
                                                     ↓
  麦克风 → 唤醒词 → ASR ─┐                  即时本地报数音频
                        ↓
Cloudflare Worker (Hono)  ← 唯一持有 DashScope API Key
  /api/voice/turn      ASR → LLM → 策略网关 → TTS
  /api/vision/review   用户主动触发的片段回顾
  /api/session/*       训练记录、XP、装备结算
  D1 (SQLite)          用户/训练/装备/记忆
```

**关键原则**：计数、报数、UI 全部本地完成，断网可用。云端只负责「说话」和「回顾」。任何模型失败都不能打断训练节奏。

### 姿态运行时（版本锁定）

```json
{ "@mediapipe/tasks-vision": "1.0.1" }
```

Apache-2.0，2026-07-31 发布。选它而不是 TF.js MoveNet 的理由：`@tensorflow-models/pose-detection` 停在 **2.1.3 / 2023-08-29**（三年未更新），而 tasks-vision 今年 7 月刚发首个稳定 1.0；33 个关键点含足部，且提供 `worldLandmarks`（米制、髋中点为原点），MoveNet 的 17 个 COCO 点做不了这些。

配置：`pose_landmarker_lite` float16（**5.6 MB**）、`delegate: "GPU"`、`runningMode: "VIDEO"`、`numPoses: 1`、`outputSegmentationMasks: false`。full 版 9.2 MB 可选，heavy 版 30 MB 不上移动端。

**WASM 与模型文件必须自托管**，不能用文档里的 jsDelivr `@latest`：一是构建不可复现，二是 MediaPipe 的 loader 要求 WASM 文件保持原名，会被带 hash 的打包器搞坏。

**角度计算一律用 `worldLandmarks`**，不用归一化坐标——后者会被画面宽高比和拍摄距离扭曲。所有评估都要先用每个点的 `visibility` / `presence` 门控：**关键点被遮挡时绝不发出动作提示**，这是最快失去用户信任的方式。

### 帧率预期（未经实测，必须验证）

Google 不公布 PoseLandmarker 的 web 端跑分。唯一可参考的实测数据来自 MoveNet：

| 设备 | Lightning / Thunder / MultiPose |
|---|---|
| iPhone 12 | 51 / 43 / 24 fps |
| Pixel 5 | **34 / 12 / 8 fps** |

Pixel 5 从最轻模型到下一档直接从 34 掉到 12 fps——余量极小。规划值：**中端安卓 lite+GPU 约 20–30fps，现代 iPhone 约 30–50fps**。中端安卓是这个方案的生死线，必须真机实测，还要测多分钟后的热降频（持续帧率会低于前 30 秒）。

### Worker 架构与 iOS 门控

`detectForVideo()` 是**同步阻塞调用**，官方文档明确建议放进 Worker。链路：主线程 `video` → 传给 Worker 的 `OffscreenCanvas` → Worker 内 GPU delegate 推理 → 关键点 post 回主线程。

但 iOS 有硬门槛，必须做特性检测 + 主线程降级：

| 能力 | 最低 iOS | 影响 |
|---|---|---|
| WASM SIMD | **16.4+** | 低于此版本回落无 SIMD 版，明显变慢 |
| OffscreenCanvas | **17.0+** | 16.2–16.7 标记为 partial，整个 iOS 16 视为不可靠 |
| OffscreenCanvas `webgl2` | **17.0+** | **Worker 架构的硬门槛**，低于此版本必须走主线程 |

iOS 上所有浏览器都是 WebKit 内核，Chrome/Edge on iOS 同样受限。

### iOS Safari 摄像头必备处理

- `playsinline` **必须加**，否则 iOS 会劫持进原生全屏播放器，而它不支持 `srcObject`。发 `autoplay playsinline muted`，并在点击回调里调 `play()`
- `getUserMedia()` 必须在**用户手势里**调用，页面加载时调常常静默失败不弹窗
- 非安全上下文下 `navigator.mediaDevices` 是 `undefined`，报的是 `TypeError` 而不是 `NotAllowedError`，两种都要处理
- iOS 不像桌面 Safari 那样持久保存授权，刷新或标签被回收后会重新弹窗，**不要假设一次授权长期有效**
- 第二次调 `getUserMedia()` 会杀掉第一个流的 track，切前后摄像头前要先 stop
- 切到后台会 mute track，要监听 `mute`/`unmute` 并在 `visibilitychange` 时重新 `play()`
- 返回的分辨率始终是横向的，读 `track.getSettings()`，不要相信自己传的 constraints
- `loadedmetadata` 之前画 canvas 得到黑帧

### 冷启动

约 11 MB WASM（SIMD 版未压缩）+ 5.6 MB 模型，还要在手机 CPU 上编译 WASM。必须显式处理：service worker 缓存优先、在「选择动作」页面就预加载、给诚实的加载态。这是移动网络下第一印象的关键。

### 为什么把 API Key 放在 Worker 里

DashScope 不允许浏览器直连（CORS + 密钥暴露）。所有模型调用必须经 Worker 代理。这是**安全要求，不是可选项**——前端打包任何 API Key 等于公开泄露。

---

## 三、模型分工

| 职责 | 模型 | 理由 |
|---|---|---|
| 对话主脑 | `qwen3.5-plus` | 陪伴、教学、组后倾听；支持工具调用与结构化输出 |
| ASR | `qwen3-asr-flash-realtime` | 中文+方言，VAD 静音阈值设 400ms；**自带 emotion 字段**可当吃力信号 |
| TTS | `qwen-audio-3.0-tts-flash` | 支持行内情绪标签 `[excited]` `[whispers]`，正好用于报数与组间调性 |
| 视觉回顾 | `qwen3-vl-flash` | 仅用户主动触发时看片段，`enable_thinking: false` |
| 兜底动作识别 | `qwen3-vl-flash` | 本地分类器 `UNKNOWN` 时抽 1 帧问一次，仍失败才开口问用户 |
| 训练计划/赛后总结（非实时） | `qwen3-max` 或 `qwq-plus` | 离开实时路径，延迟不敏感 |

不用 `qwen3-coder-*`（写代码用，运行时不需要）。

**采用串联而非端到端 omni**，理由由调研证实：`qwen3-omni-flash-realtime` 每会话**上限 8 轮音频 / 8 轮视频**，一次训练根本不够；阿里自己公布的参考数据是 omni 端到端约 5.1s，比串联的 LLM 环节（约 3.1s）**还慢约 2s**。串联另有三个好处：可用 `streaming_cancel()` 做干净打断（这是让语音「像活的」的关键）、可单独换供应商、保留字幕文本。`qwen3-omni-flash` 降为 P1 原型与降级路径。若能申请到 `qwen3.5-omni-flash-realtime`（80 轮音频 / 语义 VAD / 函数调用），这个结论要重新评估。

### Endpoint 与鉴权

```
OpenAI 兼容:  https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
ASR/TTS WS:   wss://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference
Realtime WS:  wss://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api-ws/v1/realtime?model=...
```

`{WorkspaceId}` 从百炼控制台工作空间详情页取。区域隔离：北京的 key 打新加坡的 host 返回 401 `invalid_api_key`（看起来像密钥失效，其实是区域不匹配）。Worker 与模型部署在同一区域，WebSocket RTT 直接加在响应时间上。

**服务端中继是强制的，不是风格选择**：DashScope 不返回 CORS 头，且**全平台没有临时令牌机制**（不像 OpenAI Realtime 可以签发短期客户端密钥）。WebSocket 握手虽然不受 CORS 限制，但浏览器 WebSocket 无法设置自定义 header，只能把密钥塞进 query string——公开可读。所以浏览器一律连我们自己的 Worker，Worker 再连 DashScope。中继必须校验我们自己的用户并限流，否则等于把账单开放给公网。

### 两个必须提前设计的约束

1. **`tools` 不能和 `stream: true` 同时用**。对话需要工具调用（写训练记录、查动作知识库）时不能流式。所以一轮对话拆两段：先非流式做工具决策，再流式生成自然语言。现在就按两段式写，不要等加 set-logging 时才发现。
2. **结构化输出只在非思考模式下支持**。策略网关的 Schema 依赖结构化输出，所以 `enable_thinking` 必须为 `false`（qwen3-vl-plus/flash 默认已是 false）。

### 明确不采用：持续云端视觉

调研建议开一条「持续视觉分析」链路做计数，**本方案拒绝**，理由是调研自己给出的成本数据：

| 帧尺寸 | 每帧 token | 1fps 每小时 |
|---|---|---|
| 1280×720 | ~902 | ~3.2M |
| 640×480 | ~302 | ~1.1M |
| 480×360 | ~171 | ~0.6M |

（token ≈ h×w/1024 + 2）

每训练小时烧 0.6–3.2M token 去做一件本地 MediaPipe 免费且更低延迟就能做的事，既不划算，也违反 v2.0 的 D5「视觉默认不上传」和隐私承诺。计数留在本地，视觉只在用户主动点「看看刚才」时跑一次。这条是本方案的成本护城河。

### 唤醒词 `你好动伴`

DashScope **没有唤醒词服务**（realtime 模型里的 VAD 只对「有人说话」触发，不认关键词）。浏览器也没有原生能力。调研否掉了我原先的第 2 选项：

**Web Speech API 在本产品里不可用**——不是「限制多」，是**结构性冲突**：iOS 上音频播放会终止识别器。动伴一开口说话就会掐掉自己的唤醒词监听。加上 `continuous` 自行中断、PWA/WebView 里完全不工作，这条路不能作为主路径。

修正后的分级：

| 优先级 | 方案 | 说明 |
|---|---|---|
| **P0** | 大按钮「开始」+ 按住说话 | 唯一全平台可靠路径，必须先做 |
| **P0** | AI 回应后开 10–15s 监听窗口 | 覆盖大部分「免手」价值，零唤醒词风险 |
| **P1** | Picovoice Porcupine Web | `@picovoice/porcupine-web` + `@picovoice/web-voice-processor`，WASM 跑在 Worker，中文自定义关键词免费层可训练 |

**Porcupine 两个坑**：免费层限 **3 个用户**，商用条款自相矛盾（官方博客说个人/非商用，发布报道引用官方说可商用），上线前必须自己读一遍当前 Terms；Foundation Plan 限成立 <5 年、≤20 人的初创。iOS Safari 兼容性**调研未能确认**（Picovoice 文档页被拦），必须真机验证，模拟器测不出来。

值得权衡的一点：一次训练是有边界、有意图的行为，未必需要常听唤醒。真正需要唤醒词的场景是「用户正在平板支撑、手机在三米外」。48 小时 Demo 用 P0 两条足够，唤醒词放 P1。

### 对话不跑偏

三层约束，不靠 prompt 祈祷：
1. **结构化输出 Schema**：模型必须返回 `{say_text, mode, evidence_ids, contains_prescription, contains_form_judgment}`
2. **策略网关**（确定性代码）：拦截处方式表达（加几公斤、下一组做几次）、伤病诊断、分数判定；拦截后走安全改写
3. **话题锚定**：非训练话题 → 一句话礼貌收束并把话题带回训练
4. **状态感知长度**：`SET_ACTIVE` 时只报数或极短鼓励，长对话只在组间和训练后

### 情绪价值（你强调的重点）

按事件触发，本地缓存音频优先，保证即时：

| 触发 | 表达 |
|---|---|
| 每次完成 | 本地数字音频，250ms 内播报 |
| 破个人纪录 | 「这是你目前最多的一组」+ 音效 |
| 节奏稳定 | 「节奏很稳，就这样」 |
| 明显吃力但坚持 | 「最后两个能看出来在扛，扛下来了」 |
| 检测到可能的动作偏移 | 「膝盖再往外一点点」（带不确定性，不说「你错了」） |
| 组后 | 先问感受，只问一个问题 |

---

## 四、数字分身

沿用 v2.0 的图层方案（同尺寸同锚点透明 PNG 叠加），6 个槽位：头部、上衣、下装、鞋子、配件、特效。48 小时内只做 10 件装备（按 v1.0 的 4.2 节清单）。

**体型随训练变化**（你的新需求）：不做 3D 蒙皮。用 3 档体型底图（`slim` / `normal` / `heavy`）按 BMI 选择，装备图层按体型各出一套锚点对齐的 PNG。首次进入用身高体重算 BMI 定档，后续按累计消耗热量缓慢移动档位。

素材量：3 体型 × 10 装备 = 30 张 PNG。48 小时内如果画不完，先做 `normal` 一档，另两档用 CSS `transform: scaleX()` 轻微形变兜底。

---

## 五、卡路里与奖励（确定性）

```ts
// 情况一：有心率带 — Keytel 公式
kcalPerMin = (gender === 'male')
  ? (-55.0969 + 0.6309*hr + 0.1988*weightKg + 0.2017*age) / 4.184
  : (-20.4022 + 0.4472*hr - 0.1263*weightKg + 0.074*age) / 4.184

// 情况二：无心率带 — MET 标准值
kcal = MET[exercise] * 3.5 * weightKg / 200 * minutes
```

无心率带时，MET 由**本地识别出的动作类型 + 次数密度**选取，不让大模型估算——同样输入必须得到同样结果，否则用户会发现数字对不上。

XP 用透明固定档位（按 v2.0 的 4.4 节，不奖励「动作标准度」）：完成训练 +80、完成一组并填写感受 +20、累计 100 次深蹲 +100、连续 7 天 +150、完成 10 次训练 +200。

---

## 六、工程结构

```
apps/web/                  React + Vite + TS
  src/pages/               Home / Prepare / Training / Rest / Summary / Avatar / Backpack
  src/pose/                Worker 封装 + PoseLandmarker 初始化
  src/voice/               唤醒词、录音、播放、字幕
  src/state/               训练状态机（HOME→READY→SET_ACTIVE→...）
packages/motion-core/      纯 TS：classifier / rep-counter / form-hints / calories（可单测）
packages/contracts/        API 与事件类型，前后端共享
services/api/              Cloudflare Worker + Hono + D1
  providers/               ASR / LLM / TTS / Vision Provider 接口与配置
  policy/                  策略网关
assets/avatar/             3 体型 × 图层 PNG
assets/audio/             本地数字与短鼓励音频
```

`packages/motion-core` 必须零依赖、零 IO，这样计数逻辑可以在 Node 里跑单元测试，不用开摄像头。

沿用 v2.0 第 11 章的 API 契约与事件名（`rep.completed`、`set.finished`、`reward.granted` 等）和第 12 章的数据模型，只把实现语言从 FastAPI 换成 Hono，路径与字段保持一致——这样文档不作废，后续接 React Native 也不用重写契约。

### 幂等
每个 rep 事件带 `client_event_id` (UUID)，服务端去重；开始组/结束组/奖励结算支持 `Idempotency-Key`。断网时写本地队列，恢复后按 `client_monotonic_ms` 排序补传。

---

## 七、实施顺序

| 阶段 | 内容 | 验收 |
|---|---|---|
| **0** | **中端安卓真机帧率 spike** | 裸 PoseLandmarker lite+GPU 页面，实测 fps 与 5 分钟后热降频。**这一步决定后面的 UX，必须先做** |
| 1 | 脚手架 + 手机画布 + 6 个页面可跳转 | 点击能走通全流程（数据 Mock） |
| 2 | `motion-core`：深蹲+俯卧撑计数、启发式分类、卡路里 + 单元测试 | 预录关键点序列喂进去，次数与识别结果正确（不开摄像头） |
| 3 | 摄像头 + Worker 姿态推理 + iOS 降级 + 骨骼渲染 + 本地报数 | 真人做 10 次深蹲，误差 ≤1，报数 <250ms；iOS 16 走主线程不崩 |
| 4 | Worker API + D1 + 训练记录 + XP 结算 + 幂等 | 刷新页面数据不丢；重复提交不重复加分 |
| 5 | 分身图层 + 装备解锁 + 换装 | 解锁后外观即时变化并持久化 |
| 6 | 语音链路：按住说话 → ASR → LLM（两段式）→ 策略网关 → TTS | 能问「深蹲怎么做」并得到审核内容；处方式表达被拦截 |
| 7 | 运动意图识别 + 主动询问开摄像头 + 情绪提示 | 说「我想练一下」→ AI 反问是否开摄像头 |
| 8 | P1：仰卧起坐/原地跑、卡路里显示、BLE 心率、视觉回顾、Porcupine 唤醒词 | 无设备时模拟心率有明确标记 |
| 9 | P2：开合跳、俄罗斯转体 | 调不出来就退手动计数，不硬撑 |

阶段 0 是新增的，因为帧率是唯一能推翻整个交互设计的未知数，不能放到最后才发现。阶段 1–5 是不可妥协的闭环；6 之后每一项都要有降级路径，不能阻塞演示。

**48 小时现实取舍**：只有深蹲+俯卧撑进 P0。文档 v1.0 里的六动作平铺是不现实的，因为其中两个连开源参考实现都没有。

---

## 八、主要风险

| 风险 | 应对 |
|---|---|
| **中端安卓帧率是生死线，且无人公布实测** | 阶段 0 先真机 spike；参考 Pixel 5 在 MoveNet 上 34→12fps 的悬崖，余量极小。实测 <15fps 就降分辨率或抽帧 |
| **冷启动 ~17MB** | service worker 缓存优先 + 选动作页预加载 + 诚实加载态 |
| **iOS 16 无 Worker 内 WebGL2** | 特性检测 + 主线程降级路径，不是只弹个警告 |
| **开合跳/俄罗斯转体无既有算法** | 列为 P2；俄罗斯转体在 2D 单摄下可能根本不可靠，退回手动计数 |
| 动作分类误判 | 启发式规则 + 置信度不足即 `UNKNOWN`，抽帧问 VL，仍不确定就开口问用户 |
| 遮挡导致错误提示 | 用 `visibility`/`presence` 门控，遮挡时不发提示 |
| **iOS Safari 是最大平台风险** | 唤醒词、播放中持续录音、getUserMedia in PWA 都需真机验证，第一天就 spike，模拟器无效 |
| 语音首字延迟高于对话可接受阈值 | 时间敏感的提示用**预生成短音频**，LLM 只管组间对话；用 SDK 的 `get_first_audio_delay()` 早测 |
| DashScope 延迟拖慢报数 | 报数完全本地，云端只管对话 |
| **模型定价全部未确认** | 所有文档页都把价格推给控制台。开工前从自己控制台拉 `qwen3-vl-flash`/ASR/TTS 实际单价，代入 §3 的 token 表 |
| WebSocket 生命周期 | 单会话上限 120 分钟；刚开的 socket 关闭要 5–10s（异步 close）；TTS socket 提前预热，否则首包延迟含握手时间 |
| 分身素材画不完 | 先做 normal 体型 + 10 件装备，其余用占位图 |
| Web Bluetooth 只有 Chrome/Edge 支持 | 保留模拟心率开关，UI 明确标注「模拟」 |
| 中继被滥用 | Worker 必须校验自己的用户并按用户限流；无鉴权的中继等于公开账单 |

---

## 九、验证方式

- `packages/motion-core`：`node --test`，用预录关键点序列断言次数与识别结果（不需要摄像头）
- 卡路里公式：对照 Keytel/MET 已知算例做断言
- 策略网关：固定对话集回归测试，断言处方式与评判式表达被拦截
- 端到端：手机 Chrome 打开公网 URL，真人做 10 次深蹲、10 次开合跳，核对计数、报数延迟、结算数字、装备解锁与换装持久化
- 降级演练：断网做一组、拒绝麦克风权限、拒绝摄像头权限，确认闭环仍可完成

---

## 待补充

两项调研均已完成并写入 §1 §2 §3。剩余全部是**只有你能做**的事：

1. **从百炼控制台取**：`{WorkspaceId}`，以及 `qwen3-vl-flash` / `qwen3-asr-flash-realtime` / `qwen-audio-3.0-tts-flash` 的实际单价与上下文长度（所有文档页都把价格推给控制台，查不到）
2. **真机验证（模拟器无效）**：中端安卓帧率（阶段 0）、iOS Safari 上 Porcupine 是否可用、播放语音时能否维持录音
3. **读一遍 Picovoice 当前 Terms**：免费层限 3 用户，商用条款自相矛盾，上线前必须确认
4. 可选：向阿里申请 `qwen3.5-omni-flash-realtime` 权限（80 轮音频 / 语义 VAD / 函数调用），若拿到则重新评估串联 vs 端到端
5. 待定：项目落地目录与 git 仓库位置
